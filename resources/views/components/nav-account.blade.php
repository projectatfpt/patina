<div class="card rounded-0">
    <a href="{{ $href }}"
        class="btn nav-link border-0 rounded-0 border-bottom p-3 pdcatt {{ $active ? 'bg-secondary text-white' : 'text-dark' }}">
        {{ $slot }}
    </a>
</div>
